[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_URL = "https://futurelink-ufwi.onrender.com/api"

# SPA fallback (hash routes + direct loads)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
